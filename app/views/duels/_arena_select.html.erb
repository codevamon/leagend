<%= select_tag :arena_id, 
    options_from_collection_for_select(arenas, :id, :name, selected_id),
    { 
      prompt: "Selecciona una arena...",
      class: "form-select",
      data: { 
        controller: "arena-select",
        action: "change->arena-select#selectArena"
      }
    } %>
