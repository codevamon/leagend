<h1>Duelos disponibles para jugadores libres</h1>

<div class="row">
  <div class="col-md-8">
    <% if @available_duels.any? %>
      <% @available_duels.each do |duel| %>
        <div class="card mb-4">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center">
              <h5 class="card-title">
                <%= duel.duel_type.titleize %> en <%= duel.arena&.name || "Cancha por confirmar" %>
              </h5>
              <span class="badge bg-primary">
                <%= duel.starts_at.strftime("%d/%m/%Y %H:%M") %>
              </span>
            </div>

            <div class="row mt-3">
              <div class="col-md-6">
                <h6>Equipo local</h6>
                <p>
                  <%= duel.home_team.name %><br>
                  <small class="text-muted">
                    <%= pluralize(duel.home_players.count, "jugador") %> confirmado(s)
                  </small>
                </p>
              </div>
              <div class="col-md-6">
                <h6>Equipo visitante</h6>
                <% if duel.away_team.present? %>
                  <p>
                    <%= duel.away_team.name %><br>
                    <small class="text-muted">
                      <%= pluralize(duel.away_players.count, "jugador") %> confirmado(s)
                    </small>
                  </p>
                <% else %>
                  <p class="text-muted">Equipo por confirmar</p>
                <% end %>
              </div>
            </div>

            <div class="mt-3">
              <h6>Detalles</h6>
              <ul class="list-unstyled">
                <li>
                  <i class="fas fa-clock"></i>
                  Duración: <%= duel.duration %> minutos
                </li>
                <% if duel.arena.present? %>
                  <li>
                    <i class="fas fa-map-marker-alt"></i>
                    <%= duel.arena.address %>
                  </li>
                <% end %>
              </ul>
            </div>

            <div class="mt-3">
              <%= link_to "Unirme al duelo", 
                join_free_player_path(duel_id: duel.id), 
                method: :post,
                class: "btn btn-success",
                data: { confirm: "¿Estás seguro de unirte al duelo?" }
              %>
              <%= link_to "Ver detalles", duel_path(duel), class: "btn btn-outline-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="alert alert-info">
        <h4 class="alert-heading">No hay duelos disponibles</h4>
        <p>En este momento no hay duelos abiertos para jugadores libres.</p>
        <hr>
        <p class="mb-0">
          <%= link_to "Explorar todos los duelos", explore_path, class: "btn btn-primary" %>
        </p>
      </div>
    <% end %>
  </div>

  <div class="col-md-4">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Mis duelos</h5>
        <p class="card-text">
          <%= link_to "Ver mis duelos", my_duels_free_players_path, class: "btn btn-outline-primary" %>
        </p>
      </div>
    </div>

    <div class="card mt-4">
      <div class="card-body">
        <h5 class="card-title">¿Cómo funciona?</h5>
        <ol class="card-text">
          <li>Encuentra un duelo que te interese</li>
          <li>Únete como jugador libre</li>
          <li>Serás asignado automáticamente a un equipo</li>
          <li>¡Juega y diviértete!</li>
        </ol>
      </div>
    </div>
  </div>
</div> 