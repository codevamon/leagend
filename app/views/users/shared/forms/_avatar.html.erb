<%= form_with(model: user, local: true) do |form| %>
  <div class="field">
    <%= form.label :avatar %>
    <%= form.file_field :avatar %>
  </div>

  <!-- Resto de los campos del formulario -->
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>