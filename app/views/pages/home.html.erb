
	<aside class="_home">
		<section class="_cont _1">
            <%= link_to 'New Club', new_club_path %>
            <ul>
                <% @clubs.each do |club| %>
                <li><%= link_to club.name, club_path(club) %></li>
                <% end %>
            </ul>
		</section>
	</aside> 