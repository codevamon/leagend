<h1>Clanes</h1>
<%= link_to 'Nuevo Clan', new_clan_path, class: "btn btn-outline-success mb-3" %>

<ul>
  <% @clans.each do |clan| %>
    <li><%= link_to clan.name, clan_path(clan) %></li>
  <% end %>
</ul>
